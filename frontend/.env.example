# En développement local uniquement — cible du proxy /api de Vite vers le
# backend (voir vite.config.ts). En production, le frontend est servi par
# le backend lui-même (même origine), donc rien à configurer ici.
VITE_API_PROXY_TARGET=http://localhost:3010
